<h1>Procrastination Product Categories</h1>

<% @category.each do |category| %>
  <h4><%= link_to(category.name, category_path(category.id)) %></h4></br>
<% end %>

<% form_for @category, url: categories_path do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

<%= f.submit %>

<% end %>
