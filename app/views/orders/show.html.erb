<h3><%= @order_items.count %> <%= @order_items.count == 1 ? "Item" : "Items" %> in Your Cart</h3>

<% if @order_items.count == 0 %>
  <p class="text-center">
    There are no items in your shopping cart.  Please <%= link_to "go back", root_path %> and add some items to your cart.
  </p>
<% else %>
  <table class="table">
    <tr>
      <th></th>
      <th>Name</th>
      <th>Price</th>
      <th>Quantity</th>
      <th></th>
      <th></th>
      <th>Total</th>
    </tr>
    <% @order_items.each do |item| %>
      <tr>
        <td><%= image_tag(item.product.image, class: "cart-thumbnail") %></td>
        <td><%= link_to "#{item.product.name}", merchant_product_path(merchant_id: item.product.merchant_id, id: item.product.id) %></td>
        <td><%= number_to_currency(item.product.price) %></td>
        <td><%= number_field_tag :qty, item.qty.to_i, class: "form_control", min: 1 %></td>
          <%= hidden_field_tag :product_id, item.product_id %>
          <%= hidden_field_tag :id, item.id %>
          <td><%= submit_tag "Update Quantity", class: "btn btn-default" %></td>
        </tr>
      <% end %>
        
<% end %>
